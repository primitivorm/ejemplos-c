## Install c++ in Ubuntu / Debian 

### Install dependecies
`sudo apt-get install vim g++ codelite cmake`

### Add executable flag to build.sh

`chmod +x build.sh`

### Run build.sh

`./build.sh`

### Open project with codelite

`codelite HelloWorld.workspace &`
